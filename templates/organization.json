{
    "name": "${organization_name}",
    "security": {
      "default": "none",
      "accessControlList": [
        {
          "id": "${user_email}",
          "role": "admin"
        }
      ]
    }
  }